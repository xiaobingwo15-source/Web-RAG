# Plan 4: Remove "See the Docs" Feature in Admin Page

## Complexity: ✅ Simple

This plan details the steps to remove the API Docs sidebar link ("see the docs" feature) from the admin panel interface.

## Requirements
- Remove the "API Docs" sidebar button/link from `AdminPage.tsx`.
- Remove the unused `FileJson` icon import from `lucide-react` in `AdminPage.tsx`.
- Keep the actual `/admin/scalar` route and `ApiDocsPage` implementation intact in case they are referenced/needed programmatically or for direct URL access, but ensure it is completely unreachable from the main Admin user interface.

## Detailed Tasks

### Task 1: Update `AdminPage.tsx`
- **Location**: `D:\RAG\Web-RAG\frontend\src\pages\AdminPage.tsx`
- **Actions**:
  1. Remove the JSX element for the API Docs button:
     ```tsx
     {/* API Docs link */}
     <button
       onClick={() => navigate('/admin/scalar')}
       className="flex w-full items-center gap-2 border-b border-border px-4 py-2.5 text-muted-foreground hover:text-foreground hover:bg-muted/50 transition-colors"
     >
       <FileJson className="h-4 w-4" />
       <span className="text-xs font-semibold">API Docs</span>
     </button>
     ```
  2. Remove `FileJson` import from `lucide-react` at the top of the file.

## Verification Checklist
- Run a frontend build check to ensure no compiler errors or broken import warnings:
  ```powershell
  npm run build
  ```
- Verify `AdminPage.tsx` compiles successfully.
